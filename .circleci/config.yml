version: 2.1

jobs:
  update-sample-txt:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: "git add sample.txt"
      - run: "git commit -m '[CircleCI] update sample.txt'"

workflows:
  update-sample-txt:
    jobs:
      - supdate-sample-txt
