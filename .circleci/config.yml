version: 2.1

jobs:
  xxx:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: "git add sample.txt"
      - run: "git commit -m '[CircleCI] update sample.txt'"

workflows:
  xxx:
    jobs:
      - xxx
